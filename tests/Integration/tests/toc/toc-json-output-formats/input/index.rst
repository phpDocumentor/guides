===========================
Not every format is a page
===========================

This project renders HTML, LaTeX and an inventory. Only the HTML is written
one file per page; the other two write a single file for the whole project,
so a page is named as ".html" and as nothing else.

..  toctree::

    Page
