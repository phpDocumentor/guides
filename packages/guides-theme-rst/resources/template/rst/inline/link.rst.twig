{%- if node.url -%}
    `{{ node|plaintext }} <{{- node.url -}}>`__
{%- elseif node.targetReference -%}
    :doc:`{{ node|plaintext }} <{{- node.targetReference -}}>`
{%- else -%}
    {{- node|plaintext -}}
{%- endif -%}
