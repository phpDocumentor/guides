<span{%- if node.language %} lang="{{ node.language }}"{% endif %}{%- if node.direction %} dir="{{ node.direction.value }}"{% endif %}{%- if node.class %} class="{{ node.class }}"{% endif %}>{{- node.value -}}</span>
