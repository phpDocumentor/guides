<div class="section{% if node.classes %} {{ node.classesString }}{% endif %}" id="{{ node.title.id }}"{{ renderDataAttributes(node) }}>
    {% for childNode in node.children %}
        {{ renderNode(childNode) }}
    {% endfor %}
</div>
