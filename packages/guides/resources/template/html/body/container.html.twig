<div class="{{ class }}"{% if id is defined and id %} id="{{ id }}"{% endif %}{% if lang is defined and lang %} lang="{{ lang }}"{% endif %}{% if dir is defined and dir %} dir="{{ dir }}"{% endif %}>
    {{ renderNode(node) }}
</div>
{# force a new line at the end of the file #}
