{% if node.isLink %}
{{ renderNode(node.reference) }}
{% else %}
<span class="index-see">{{ node.isSeeAlso ? 'see also' : 'see' }}</span>
{% if node.reference %}
{{ renderNode(node.reference) }}
{% else %}
{{ node.seeText }}
{% endif %}
{% endif %}
