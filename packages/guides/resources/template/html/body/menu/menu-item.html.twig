<li class="toc-item"><a href="{{ renderLink(node.file) }}">{{ node.title }}</a>

    {%- if node.children|length %}
        {% include "body/menu/menu-level.html.twig" with {
            tocItems:tocItem.children
        } %}
    {%- endif %}
</li>
