<li class="toc-item">
    <a href="{{ url }}">{{ renderNode(node.value.value) }}</a>

    {% if node.children|length %}
        {% include "guides/body/toc/toc-level.html.twig" with {
            tocItems:tocItem.children
        } %}
    {% endif %}
</li>
